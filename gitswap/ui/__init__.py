"""
gitswap/ui
----------
tkinter-based graphical interface for Git-Swap.

Requires Python to be built with tkinter support (standard on macOS).
The core package (gitswap.*) is importable without tkinter — only this
sub-package has a GUI dependency.
"""
